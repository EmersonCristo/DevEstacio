#include <stdio.h> // PRECISO INSTALAR O C CORRETAMENTE NA MINHA MÁQUINA
#include <stdbool.h> // Necessário para usar o tipo 'bool'

int main() {
    bool usuario_logado = false; // O usuário não está logado

    // Condição: Se NÃO for usuário_logado (ou seja, se for falso)
    if (!usuario_logado) { //Se NÃO for o usuario _logado  
        printf("Aviso: O usuário deve fazer login para continuar.\n");
    } else {
        printf("Bem-vindo!\n");
    }

    #include <stdio.h>
#include <stdbool.h>
 
